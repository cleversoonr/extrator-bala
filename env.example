# ========================================
# CONFIGURAÇÃO AZURE OPENAI (Recomendado)
# ========================================
AZURE_OPENAI_ENDPOINT=https://mathe-mhp26atv-eastus2.cognitiveservices.azure.com/
AZURE_OPENAI_API_KEY=sua_chave_aqui
AZURE_OPENAI_DEPLOYMENT=gpt-5        # Nome do deployment (sem espaços)
AZURE_OPENAI_API_VERSION=2025-04-01-preview

# (Opcional) Modelo BARATO para pré-check no Azure (ex.: gpt-4.1 ou gpt-4o-mini)
AZURE_OPENAI_PRECHECK_ENDPOINT=https://digifarmz-ai.openai.azure.com/
AZURE_OPENAI_PRECHECK_API_KEY=sua_chave_modelo_barato
AZURE_OPENAI_PRECHECK_DEPLOYMENT=gpt41-cheap          # Nome exato do deployment de pré-check
AZURE_OPENAI_PRECHECK_API_VERSION=2025-10-03-preview
# Se você não definir estes campos, o pre-check continua habilitado usando o mesmo deployment principal.

# Paralelismo: quantas páginas enviar simultaneamente (requer crédito suficiente)
LLM_MAX_WORKERS=6

# ⚠️ IMPORTANTE: 
# - AZURE_OPENAI_DEPLOYMENT deve ser o NOME DO DEPLOYMENT no Azure Portal
# - Pode ser gpt-5, gpt-4o, gpt-4-turbo, etc (o nome que você deu ao deployment)
# - Vá em Azure Portal > Azure OpenAI > Deployments e copie o nome exato

# ========================================
# ALTERNATIVAS
# ========================================

# OpenAI Público
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o

# OpenRouter
# OPENROUTER_API_KEY=sk-...
# OPENROUTER_MODEL=openai/gpt-4o
